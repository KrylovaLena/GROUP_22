
 1. Вывести всех работников чьи зарплаты есть в базе, вместе с зарплатами. (24 значения)

select employees.employee_name, employees_salary.monthly_salary 
from employees
inner join employees_salary
on employees.id = employees_salary.employee_id;


 2. Вывести всех работников у которых ЗП меньше 2000. (17 значений)

select employees.employee_name, employees_salary.monthly_salary 
from employees
inner join employees_salary
on employees.id = employees_salary.employee_id
where employees_salary.monthly_salary < 2000;

 3. Вывести все зарплатные позиции, но работник по ним не назначен. (ЗП есть, но не понятно кто её получает.) (11 значений)

select monthly_salary
from employees
full join
employees_salary 
on employees.id = employees_salary.employee_id 
where employee_name is NULL;

 4. Вывести все зарплатные позиции  меньше 2000 но работник по ним не назначен. (ЗП есть, но не понятно кто её получает.)(5 значений)

select monthly_salary
from employees
full join
employees_salary 
on employees.id = employees_salary.employee_id 
where employee_name is null and employees_salary.monthly_salary < 2000;

 5. Найти всех работников кому не начислена ЗП. (29 значений)

select employee_name 
from employees
full join
employees_salary 
on employees.id = employees_salary.employee_id 
where employees_salary.monthly_salary is null;

 6. Вывести всех работников с названиями их должности. (40 значений)

select employee_name, roles.role_name
from employees, roles_employees, roles
where employees.id=roles_employees.employee_id and roles.id=roles_employees.role_id ;

select employee_name, roles.role_name
from roles_employees
join employees
on employees.id=roles_employees.employee_id
join roles
on roles.id=roles_employees.role_id;

 7. Вывести имена и должность только Java разработчиков.(7 значений)

select employees.employee_name, roles.role_name
from employees, roles_employees, roles
where employees.id=roles_employees.employee_id and roles.id=roles_employees.role_id and roles.role_name like '%Java %';


select employee_name, roles.role_name
from roles_employees
join employees
on employees.id=roles_employees.employee_id
join roles
on roles.id=roles_employees.role_id
WHERE roles.role_name like '%Java %';

 8. Вывести имена и должность только Python разработчиков. (3 значения)

select employee_name, roles.role_name
from employees, roles_employees, roles
where employees.id=roles_employees.employee_id and roles.id=roles_employees.role_id and roles.role_name like '%Python%';


select employee_name, roles.role_name
from roles_employees
join employees
on employees.id=roles_employees.employee_id
join roles
on roles.id=roles_employees.role_id
WHERE roles.role_name like '%Python %';

 9. Вывести имена и должность всех QA инженеров.(13 значений)

select employee_name, roles.role_name
from employees, roles_employees, roles
where employees.id=roles_employees.employee_id and roles.id=roles_employees.role_id and roles.role_name like '%QA%';


select employee_name, roles.role_name
from roles_employees
join employees
on employees.id=roles_employees.employee_id
join roles
on roles.id=roles_employees.role_id
WHERE roles.role_name like '%QA%';

 10. Вывести имена и должность ручных QA инженеров.(10 значений)

select employee_name, roles.role_name
from employees, roles_employees, roles
where employees.id=roles_employees.employee_id and roles.id=roles_employees.role_id and roles.role_name like '%Manual QA%';


select employee_name, roles.role_name
from roles_employees
join employees
on employees.id=roles_employees.employee_id
join roles
on roles.id=roles_employees.role_id
WHERE roles.role_name like '%Manual QA%';

 11. Вывести имена и должность автоматизаторов QA (3 значения)

select employee_name, roles.role_name
from employees, roles_employees, roles
where employees.id=roles_employees.employee_id and roles.id=roles_employees.role_id and roles.role_name like '%Aut% %QA%';


select employee_name, roles.role_name
from roles_employees
join employees
on employees.id=roles_employees.employee_id
join roles
on roles.id=roles_employees.role_id
WHERE roles.role_name like '%Aut% %QA%';

 12. Вывести имена и зарплаты Junior специалистов (5 значений)

select employees.employee_name, employees_salary.monthly_salary
from employees
join employees_salary
on employees.id = employees_salary.employee_id 
join roles_employees
on employees.id =  roles_employees.employee_id 
join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%Junior%' ;


 13. Вывести имена и зарплаты Middle специалистов (4 значения)

select employees.employee_name, employees_salary.monthly_salary
from employees
join employees_salary
on employees.id = employees_salary.employee_id 
join roles_employees
on employees.id =  roles_employees.employee_id 
join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%Middle%' ;

 14. Вывести имена и зарплаты Senior специалистов (4 значения)

select employees.employee_name, employees_salary.monthly_salary
from employees
join employees_salary
on employees.id = employees_salary.employee_id 
join roles_employees
on employees.id =  roles_employees.employee_id 
join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%Senior%' ;

 15. Вывести зарплаты Java разработчиков (3 значения)

select employees_salary.monthly_salary
from employees
join employees_salary
on employees.id = employees_salary.employee_id 
join roles_employees
on employees.id =  roles_employees.employee_id 
join roles 
on roles.id =roles_employees.role_id 
WHERE roles.role_name like '%Java %';


 16. Вывести зарплаты Python разработчиков (2 значения)

select employees_salary.monthly_salary
from employees
join employees_salary
on employees.id = employees_salary.employee_id 
join roles_employees
on employees.id =  roles_employees.employee_id 
join roles 
on roles.id =roles_employees.role_id 
WHERE roles.role_name like '%Python %';

 17. Вывести имена и зарплаты Junior Python разработчиков (0 значений)

select employees.employee_name, employees_salary.monthly_salary
from employees
full join employees_salary
on employees.id = employees_salary.employee_id 
full join roles_employees
on employees.id =  roles_employees.employee_id 
full join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%Junior Python%' ;

 18. Вывести имена и зарплаты Middle JS разработчиков (2 значения)

select employees.employee_name, employees_salary.monthly_salary
from employees
full join employees_salary
on employees.id = employees_salary.employee_id 
full join roles_employees
on employees.id =  roles_employees.employee_id 
full join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%Middle JavaS%' ;

 19. Вывести имена и зарплаты Senior Java разработчиков (3 значения)

select employees.employee_name, employees_salary.monthly_salary
from employees
full join employees_salary
on employees.id = employees_salary.employee_id 
full join roles_employees
on employees.id =  roles_employees.employee_id 
full join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%Senior Java %' ;

 20. Вывести зарплаты Junior QA инженеров (13 значений)

 select employees_salary.monthly_salary
from employees
full join employees_salary
on employees.id = employees_salary.employee_id 
full join roles_employees
on employees.id =  roles_employees.employee_id 
full join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%Junior%' and roles.role_name like '%QA%' ;

 21. Вывести среднюю зарплату всех Junior специалистов (1360)

select AVG(employees_salary.monthly_salary)
from employees
full join employees_salary
on employees.id = employees_salary.employee_id 
full join roles_employees
on employees.id =  roles_employees.employee_id 
full join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%Junior%' ;

 22. Вывести сумму зарплат JS разработчиков (2550)

select SUM(employees_salary.monthly_salary)
from employees
full join employees_salary
on employees.id = employees_salary.employee_id 
full join roles_employees
on employees.id =  roles_employees.employee_id 
full join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%JavaS%' ;


 23. Вывести минимальную ЗП QA инженеров (900)

select min(employees_salary.monthly_salary)
from employees
full join employees_salary
on employees.id = employees_salary.employee_id 
full join roles_employees
on employees.id =  roles_employees.employee_id 
full join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%QA%' ;

 24. Вывести максимальную ЗП QA инженеров (1850)

select max(employees_salary.monthly_salary)
from employees
full join employees_salary
on employees.id = employees_salary.employee_id 
full join roles_employees
on employees.id =  roles_employees.employee_id 
full join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%QA%' ;

 25. Вывести количество QA инженеров (13)

select count(employees.id)
from employees
full join employees_salary
on employees.id = employees_salary.employee_id 
full join roles_employees
on employees.id =  roles_employees.employee_id 
full join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%QA%' ;

 26. Вывести количество Middle специалистов. (9)

select count(employees.id)
from employees
full join employees_salary
on employees.id = employees_salary.employee_id 
full join roles_employees
on employees.id =  roles_employees.employee_id 
full join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%Middle%' ;

 27. Вывести количество разработчиков (17)

select count(employees.id)
from employees
full join employees_salary
on employees.id = employees_salary.employee_id 
full join roles_employees
on employees.id =  roles_employees.employee_id 
full join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%developer%' ;

 28. Вывести фонд (сумму) зарплаты разработчиков. (10800)

select sum(employees_salary.monthly_salary)
from employees
full join employees_salary
on employees.id = employees_salary.employee_id 
full join roles_employees
on employees.id =  roles_employees.employee_id 
full join roles 
on roles.id =roles_employees.role_id 
where roles.role_name like '%developer%' ;

 29. Вывести имена, должности и ЗП всех специалистов по возрастанию (21)

select employees.employee_name, roles.role_name, employees_salary.monthly_salary
from employees
join employees_salary
on employees.id = employees_salary.employee_id 
join roles_employees
on employees.id =  roles_employees.employee_id 
join roles 
on roles.id =roles_employees.role_id 
order by employees_salary.monthly_salary;


select employees.employee_name, roles.role_name, employees_salary.monthly_salary
from employees
left join employees_salary
on employees.id = employees_salary.employee_id 
left join roles_employees
on employees.id =  roles_employees.employee_id 
left join roles 
on roles.id =roles_employees.role_id 
order by employees_salary.monthly_salary;

 30. Вывести имена, должности и ЗП всех специалистов по возрастанию у специалистов у которых ЗП от 1700 до 2300 (7 значений)

select employees.employee_name, roles.role_name, employees_salary.monthly_salary
from employees
join employees_salary
on employees.id = employees_salary.employee_id 
join roles_employees
on employees.id =  roles_employees.employee_id 
join roles 
on roles.id =roles_employees.role_id
where employees_salary.monthly_salary>1700 and employees_salary.monthly_salary<2300
order by employees_salary.monthly_salary;

select employees.employee_name, roles.role_name, employees_salary.monthly_salary
from employees
left join employees_salary
on employees.id = employees_salary.employee_id 
left join roles_employees
on employees.id =  roles_employees.employee_id 
left join roles 
on roles.id =roles_employees.role_id 
where employees_salary.monthly_salary>1700 and employees_salary.monthly_salary<2300
order by employees_salary.monthly_salary;

 31. Вывести имена, должности и ЗП всех специалистов по возрастанию у специалистов у которых ЗП меньше 2300 (18 значений)

select employees.employee_name, roles.role_name, employees_salary.monthly_salary
from employees
join employees_salary
on employees.id = employees_salary.employee_id 
join roles_employees
on employees.id =  roles_employees.employee_id 
join roles 
on roles.id =roles_employees.role_id
where employees_salary.monthly_salary<2300
order by employees_salary.monthly_salary;

select employees.employee_name, roles.role_name, employees_salary.monthly_salary
from employees
left join employees_salary
on employees.id = employees_salary.employee_id 
left join roles_employees
on employees.id =  roles_employees.employee_id 
left join roles 
on roles.id =roles_employees.role_id 
where employees_salary.monthly_salary<2300
order by employees_salary.monthly_salary;

 32. Вывести имена, должности и ЗП всех специалистов по возрастанию у специалистов у которых ЗП равна 1100, 1500, 2000 (4 значения)

select employees.employee_name, roles.role_name, employees_salary.monthly_salary
from employees
join employees_salary
on employees.id = employees_salary.employee_id 
join roles_employees
on employees.id =  roles_employees.employee_id 
join roles 
on roles.id =roles_employees.role_id
where employees_salary.monthly_salary in (1100, 1500, 2000) 
order by employees_salary.monthly_salary;

select employees.employee_name, roles.role_name, employees_salary.monthly_salary
from employees
left join employees_salary
on employees.id = employees_salary.employee_id 
left join roles_employees
on employees.id =  roles_employees.employee_id 
left join roles 
on roles.id =roles_employees.role_id
where employees_salary.monthly_salary in (1100, 1500, 2000) 
order by employees_salary.monthly_salary;



